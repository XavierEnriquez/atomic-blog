import { createContext } from "react";

// 1) We create Context
// createContext returns a component and like all components they should be capitilized
const PostContext = createContext();

export default PostContext;
